diff --git a/android/src/main/java/com/whitestein/securestorage/PasswordStorageHelper.java b/android/src/main/java/com/whitestein/securestorage/PasswordStorageHelper.java
index 1ee07cb..5ba28f1 100644
--- a/android/src/main/java/com/whitestein/securestorage/PasswordStorageHelper.java
+++ b/android/src/main/java/com/whitestein/securestorage/PasswordStorageHelper.java
@@ -48,23 +48,27 @@ public class PasswordStorageHelper {
     private PasswordStorageImpl passwordStorage = null;
 
     public PasswordStorageHelper(Context context) {
-        if (android.os.Build.VERSION.SDK_INT < 18) {
-            passwordStorage = new PasswordStorageHelper_SDK16();
-        } else {
-            passwordStorage = new PasswordStorageHelper_SDK18();
+        // EZTerminal stores a bearer token here. Never fall back to the
+        // plugin's base64-only SDK16 SharedPreferences backend, including
+        // when AndroidKeyStore initialization fails on a newer device.
+        if (android.os.Build.VERSION.SDK_INT < 18) {
+            throw new IllegalStateException("AndroidKeyStore requires API 18 or newer");
         }
+        passwordStorage = new PasswordStorageHelper_SDK18();
 
         boolean isInitialized = false;
+        RuntimeException initializationError = null;
 
         try {
             isInitialized = passwordStorage.init(context);
         } catch (Exception ex) {
             Log.e(LOG_TAG, "PasswordStorage initialisation error:" + ex.getMessage(), ex);
+            initializationError = new IllegalStateException("AndroidKeyStore initialization failed", ex);
         }
 
-        if (!isInitialized && passwordStorage instanceof PasswordStorageHelper_SDK18) {
-            passwordStorage = new PasswordStorageHelper_SDK16();
-            passwordStorage.init(context);
+        if (!isInitialized) {
+            if (initializationError != null) throw initializationError;
+            throw new IllegalStateException("AndroidKeyStore initialization failed");
         }
     }
 
diff --git a/android/src/main/java/com/whitestein/securestorage/SecureStoragePluginPlugin.java b/android/src/main/java/com/whitestein/securestorage/SecureStoragePluginPlugin.java
--- a/android/src/main/java/com/whitestein/securestorage/SecureStoragePluginPlugin.java
+++ b/android/src/main/java/com/whitestein/securestorage/SecureStoragePluginPlugin.java
@@ -59,12 +59,9 @@ public class SecureStoragePluginPlugin extends Plugin {
     public void remove(PluginCall call) {
         String key = call.getString("key");
         try {
-            if(this.has(key)) {
-                call.resolve(this._remove(key));
-            }
-            else {
-                call.reject("Item with given key does not exist");
-            }
+            // Delete by key without decrypting first. This keeps an unreadable
+            // encrypted entry recoverable through an explicit app-level discard.
+            call.resolve(this._remove(key));
         } catch (Exception exception) {
             call.reject(exception.getMessage(), exception);
         }
